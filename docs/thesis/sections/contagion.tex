\documentclass[../main.tex]{subfiles}
\section{Contagion on the Model}

As equation (\ref{local_p_comp}) shows, price contagion depends entirely on the matrix $(2\matr{I} + \matr{G})^{-1}$ which we can define as the ``bargaining power'' matrix, since it allocates revenues the excess revenues, $\Delta_t$, across the network.